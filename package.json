{
  "name": "nora-kosmetik",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i tailwind.input.css -o styles.css --minify",
    "watch": "tailwindcss -i tailwind.input.css -o styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
