{
  "name": "nora-gerstenberg",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i tailwind.input.css -o styles.css --minify",
    "start": "serve . -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
